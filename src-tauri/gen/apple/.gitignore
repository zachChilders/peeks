xcuserdata/
build/
Externals/
# Tauri copies `bundle.resources` in here at build time. The directory itself is declared
# as a resources build phase in project.yml and has to exist, but its contents are copies
# of files that live in src-tauri/resources.
assets/*
!assets/.gitkeep
